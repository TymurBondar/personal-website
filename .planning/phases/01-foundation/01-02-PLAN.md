---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/components/Navbar.js
  - src/components/Footer.js
  - src/app/page.js
  - src/app/layout.js
  - src/app/about/page.js
  - src/app/portfolio/page.js
autonomous: true
requirements:
  - STACK-03
  - STACK-05

must_haves:
  truths:
    - "No DaisyUI class names remain in any source file (no btn, navbar, dropdown, bg-base-*, menu, hero, footer, timeline, card, rounded-box, text-neutral-content, bg-neutral)"
    - "The Navbar mobile menu opens and closes via a click handler using React useState"
    - "The site builds without errors and renders all pages"
    - "The Navbar has 'use client' directive (only component that needs it)"
  artifacts:
    - path: "src/components/Navbar.js"
      provides: "Interactive navbar with React state mobile menu"
      contains: "useState"
      min_lines: 30
    - path: "src/components/Footer.js"
      provides: "Footer with plain Tailwind classes"
      contains: "flex"
    - path: "src/app/page.js"
      provides: "Home page with plain Tailwind layout"
      contains: "flex"
    - path: "src/app/layout.js"
      provides: "Root layout with plain Tailwind bg class"
      contains: "bg-gray-900"
  key_links:
    - from: "src/components/Navbar.js"
      to: "useState"
      via: "React hook import"
      pattern: "import.*useState.*from.*react"
    - from: "src/components/Navbar.js"
      to: "isMenuOpen"
      via: "State toggle on click"
      pattern: "setIsMenuOpen"
    - from: "src/app/layout.js"
      to: "src/components/Navbar.js"
      via: "Component import"
      pattern: "import Navbar"
---

<objective>
Replace all DaisyUI semantic classes with plain Tailwind v4 utilities across every component. Rebuild the Navbar as a client component with React useState for the mobile menu toggle. After this plan, zero DaisyUI class names remain in the codebase.

Purpose: Completes the DaisyUI removal so the site renders correctly on the new Tailwind v4 stack with a working mobile menu.
Output: All components use plain Tailwind utilities. The Navbar mobile dropdown works via click handler. The site builds and renders with a basic dark theme.
</objective>

<execution_context>
@/Users/tymurbondar/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tymurbondar/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- DaisyUI class mappings from RESEARCH.md — executor uses these directly -->

Navbar.js class replacements:
  navbar                    -> flex items-center justify-between
  bg-base-300               -> bg-gray-900
  btn btn-ghost             -> text-2xl sm:text-3xl font-bold text-gray-100 (logo)
  btn btn-ghost sm:hidden   -> sm:hidden text-gray-100 p-1 (hamburger)
  dropdown dropdown-end     -> relative (wrapper for hamburger + dropdown)
  menu menu-sm dropdown-content mt-3 z-[1] shadow bg-base-200 rounded-box p-0
                            -> absolute top-14 right-0 bg-gray-800 rounded shadow-sm p-2 flex flex-col z-10
  menu menu-horizontal px-1 text-lg lg:text-xl
                            -> hidden sm:flex gap-6 text-lg text-gray-100

Footer.js class replacements:
  footer p-2 md:p-4 items-center bg-neutral text-neutral-content
                            -> flex p-2 md:p-4 items-center bg-gray-900 text-gray-100

layout.js class replacement:
  bg-base-100               -> bg-gray-900

page.js class replacements:
  hero                      -> flex items-center justify-center
  hero-content flex-col lg:flex-row-reverse pt-7 lg:py-0
                            -> flex flex-col lg:flex-row-reverse items-center gap-8 pt-7 lg:py-0 px-4

About page (about/page.js):
  timeline, timeline-*, text-success, text-accent, bg-success, bg-accent
                            -> Replace with plain Tailwind equivalents (this page is removed in Phase 2, just make it not crash)

Portfolio page (portfolio/page.js):
  card, card-body, card-title, card-actions, btn btn-primary, bg-base-100, shadow-xl
                            -> Replace with plain Tailwind equivalents (this page is removed in Phase 2, just make it not crash)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild Navbar with React useState and plain Tailwind</name>
  <files>src/components/Navbar.js</files>
  <action>
Completely rewrite Navbar.js as a client component with React state for the mobile menu. The component must:

1. Add `'use client';` directive at the top of the file.
2. Import `{ useState }` from `'react'` and `Link` from `'next/link'`.
3. Use `const [isMenuOpen, setIsMenuOpen] = useState(false);` for mobile toggle.
4. Structure (mobile menu approach is Claude's discretion — use an absolute-positioned dropdown):

```jsx
'use client';
import { useState } from 'react';
import Link from 'next/link';

export default function Navbar() {
  const [isMenuOpen, setIsMenuOpen] = useState(false);

  return (
    <nav className="relative flex items-center justify-between px-4 py-3 bg-gray-900">
      {/* Logo */}
      <Link href="/" className="text-2xl sm:text-3xl font-bold text-gray-100">
        Tymur Bondar
      </Link>

      {/* Desktop nav — hidden on mobile */}
      <ul className="hidden sm:flex gap-6 text-lg text-gray-100">
        <li><Link href="/about">About Me</Link></li>
        <li><Link href="/portfolio">Projects</Link></li>
      </ul>

      {/* Hamburger — visible on mobile only */}
      <button
        className="sm:hidden text-gray-100 p-1"
        onClick={() => setIsMenuOpen(!isMenuOpen)}
        aria-label="Toggle menu"
      >
        <svg className="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
          <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2"
            d="M4 6h16M4 12h8m-8 6h16" />
        </svg>
      </button>

      {/* Mobile dropdown — conditionally rendered */}
      {isMenuOpen && (
        <ul className="absolute top-14 right-4 bg-gray-800 rounded shadow-sm p-2 flex flex-col gap-1 z-10">
          <li>
            <Link href="/about" className="block px-4 py-2 text-gray-100 text-lg whitespace-nowrap">
              About Me
            </Link>
          </li>
          <li>
            <Link href="/portfolio" className="block px-4 py-2 text-gray-100 text-lg">
              Projects
            </Link>
          </li>
        </ul>
      )}
    </nav>
  );
}
```

Key requirements:
- `relative` on the `<nav>` element (so the absolute dropdown positions correctly)
- `sm:hidden` on hamburger button (only shows on mobile)
- `hidden sm:flex` on desktop nav (only shows on desktop)
- The dropdown uses `absolute top-14 right-4` for positioning below the nav
- NO DaisyUI classes: no `navbar`, `btn`, `btn-ghost`, `dropdown`, `dropdown-end`, `menu`, `menu-sm`, `dropdown-content`, `bg-base-*`, `rounded-box`
- NO `tabIndex` focus trick — pure React state
  </action>
  <verify>
    <automated>cd /Users/tymurbondar/Development/web/personal-website/.claude/worktrees/sweet-murdock && grep -c "useState" src/components/Navbar.js && grep -c "'use client'" src/components/Navbar.js && ! grep -qE "navbar|btn|dropdown|menu|bg-base|rounded-box" src/components/Navbar.js && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Navbar.js is a 'use client' component using useState for mobile menu toggle. Zero DaisyUI classes remain. Has relative positioning on nav, sm:hidden hamburger, hidden sm:flex desktop nav, absolute dropdown.</done>
</task>

<task type="auto">
  <name>Task 2: Replace DaisyUI classes in Footer, Home page, layout, About, and Portfolio</name>
  <files>src/components/Footer.js, src/app/page.js, src/app/layout.js, src/app/about/page.js, src/app/portfolio/page.js</files>
  <action>
Replace DaisyUI semantic classes with plain Tailwind utilities in all remaining files. Use the exact mappings from the research.

**Footer.js** — Replace the outer `<footer>` classes:
- `footer p-2 md:p-4 items-center bg-neutral text-neutral-content`
  becomes `flex p-2 md:p-4 items-center bg-gray-900 text-gray-100`
- Keep all inner structure (nav flex, social SVG icons) exactly as-is — they use no DaisyUI classes.

**layout.js** — Replace the single DaisyUI class on `<main>`:
- `bg-base-100 flex-grow content-center`
  becomes `bg-gray-900 flex-grow content-center`
- Everything else in layout.js stays the same (Inter font, metadata, Navbar/Footer imports).

**page.js (Home)** — Replace hero classes:
- Outer div: `hero` becomes `flex items-center justify-center`
- Inner div: `hero-content flex-col lg:flex-row-reverse pt-7 lg:py-0`
  becomes `flex flex-col lg:flex-row-reverse items-center gap-8 pt-7 lg:py-0 px-4`
- Keep Image component and text content exactly as-is.

**about/page.js** — Replace timeline DaisyUI classes with minimal Tailwind. This page is being removed in Phase 2 — the goal is just "doesn't crash, no DaisyUI classes." Replace:
- `timeline timeline-snap-icon max-sm:timeline-compact timeline-vertical items-center`
  with `flex flex-col items-center gap-4`
- `timeline-middle` with `flex items-center justify-center`
- `timeline-start` with `text-right w-72` (keep sm:text-end)
- `timeline-end` with `w-72`
- `text-success` with `text-green-500`
- `text-accent` with `text-blue-400`
- `bg-success` with `bg-green-500`
- `bg-accent` with `bg-blue-400`
- Remove `<hr>` tags with bg-success/bg-accent (they relied on DaisyUI timeline connector styling and will just render as plain lines — acceptable for a page being deleted in Phase 2).
  Or: simplify the `<hr>` tags to `<hr className="w-0.5 h-8 bg-green-500 border-none" />` as simple vertical connectors.

**portfolio/page.js** — Replace card DaisyUI classes with minimal Tailwind. Also being removed in Phase 2. Replace:
- `card w-80 bg-base-100 shadow-xl` with `w-80 bg-gray-800 rounded-lg overflow-hidden shadow-lg`
- `card-body` with `p-4`
- `card-title` with `text-xl font-bold`
- `card-actions justify-end` with `flex justify-end mt-2`
- `btn btn-primary` with `px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700`

After all replacements, run a comprehensive DaisyUI class audit:
```bash
grep -rn "navbar\|bg-base-\|btn\|dropdown\|menu\|hero\|footer \|bg-neutral\|text-neutral-content\|timeline\|card-body\|card-title\|card-actions\|rounded-box\|text-success\|text-accent\|bg-success\|bg-accent" src/
```
This MUST return zero matches. If any remain, fix them.

Then run `yarn build` to confirm the full project compiles.
  </action>
  <verify>
    <automated>cd /Users/tymurbondar/Development/web/personal-website/.claude/worktrees/sweet-murdock && ! grep -rqE "navbar|bg-base-|btn |btn-ghost|btn-primary|dropdown|menu |menu-sm|menu-horizontal|hero |hero-content|footer |bg-neutral|text-neutral-content|timeline|card-body|card-title|card-actions|rounded-box|text-success|text-accent|bg-success|bg-accent" src/ && yarn build 2>&1 | tail -3 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Zero DaisyUI class names in any source file under src/. All components use plain Tailwind v4 utilities. `yarn build` completes without errors. The site renders with a basic dark theme — "just functional" per Phase 1 requirements.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `grep -rn "navbar\|bg-base-\|btn\|dropdown\|menu\|hero\|footer \|bg-neutral\|text-neutral-content\|timeline\|card\|rounded-box" src/` returns zero results
2. `src/components/Navbar.js` contains `'use client'` and `useState`
3. Navbar mobile menu opens/closes via click (React state, not CSS focus)
4. `yarn build` exits 0
5. No DaisyUI semantic classes anywhere in the component tree
</verification>

<success_criteria>
Every DaisyUI semantic class has been replaced with a plain Tailwind v4 utility. The Navbar mobile menu uses React useState for toggling. The site builds and renders on the modern stack. Visual quality is "just functional" — correct basic dark theme, functional layout, no polish (per user's locked decision).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
